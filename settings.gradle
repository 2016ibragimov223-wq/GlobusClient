pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPODOCS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "GlobusClient"
include ':app'
