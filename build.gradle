// language: groovy, file: build.gradle, target: Android Project Root
plugins {
    id 'com.android.application' version '8.2.0' apply false
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
